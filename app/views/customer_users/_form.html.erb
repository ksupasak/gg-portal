<%= form_with(model: customer_user) do |form| %>
  <% if customer_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(customer_user.errors.count, "error") %> prohibited this customer_user from being saved:</h2>

      <ul>
        <% customer_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= form.label :customer_id %>
    <%= form.select :customer_id, options_for_select(Customer.all.collect{|i| [i.name,i.id]},customer_user.customer_id) %>
  </div>
  
  

  <div class="field">
    <%= form.label :user_id %>
    <%= form.select :user_id, options_for_select(User.all.collect{|i| [i.name,i.id]},customer_user.user_id) %>
  </div>

  <div class="field">
    <%= form.label :registered_at %>
    <%= form.text_field :registered_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
<%=form_css%>

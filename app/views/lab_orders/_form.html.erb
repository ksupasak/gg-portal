<%= form_with(model: lab_order) do |form| %>
  <% if lab_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lab_order.errors.count, "error") %> prohibited this lab_order from being saved:</h2>

      <ul>
        <% lab_order.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :customer_id %>
    <%= form.text_field :customer_id %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.datetime_select :date %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_field :note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

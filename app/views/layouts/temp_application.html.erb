<!DOCTYPE html>
<html>
  <head>
    <title>GgPortal</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
	  <h1>GG</h1>
	  <% if user_signed_in? %>
	  
	  <%= current_user.inspect %>
	  
	  <a data-confirm="Are you sure?" data-method="delete" href="/users/sign_out" rel="nofollow">Sign out</a>
	  <% else %>
	  <a   href="/users/sign_in" rel="nofollow">Sign in</a>
	  
	  <% end %>
    <%= yield %>
  </body>
</html>
